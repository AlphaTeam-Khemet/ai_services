# Pinned versions — update intentionally, test after any upgrade
#
# GPU-ENABLED: PyTorch with CUDA 12.1 support for fast embedding on GPU.
# The container requires the NVIDIA runtime (--gpus all) and nvidia-container-toolkit
# on the host. Falls back to CPU automatically if no GPU is available.
--extra-index-url https://download.pytorch.org/whl/cu121
torch

# ── RAG / Embeddings ───────────────────────────────────────────────────────────
sentence-transformers>=2.7.0
chromadb>=0.5.0

# ── LLM Client ────────────────────────────────────────────────────────────────
groq>=0.9.0

# ── API Framework ─────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0

# NOTE: langchain was removed — it is not used anywhere in this service.

# ── Voice Tour Guide ──────────────────────────────────────────────────────────
# REMOVED — voice narration has been moved to AI_services/voice_tour_guide/
# elevenlabs, sqlalchemy, httpx are no longer needed here.
